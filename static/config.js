// Configure API base URL for the frontend.
// Change this to your server URL when packaging the app (e.g. 'https://api.example.com/api').
var API_BASE_URL = '/api';

// When using a packaged native app that can't use relative paths,
// set API_BASE_URL to the full path of your hosted Flask API.
